# Sourced automatically by oh-my-zsh from $ZSH_CUSTOM/*.zsh.
if [[ -d /usr/local/go/bin ]]; then
    # $HOME/go/bin is the default GOBIN, where "go install" drops binaries.
    export PATH="/usr/local/go/bin:$HOME/go/bin:$PATH"
fi
